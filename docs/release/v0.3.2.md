# KUC v0.3.2 統合リリース

KUC `v0.3.2` は、このリリース対象として追跡されている KUC の全 open issue
（#13、#21、#22、#23、#24、#28）を統合する。対象は generic gesture input、
issue-first release hygiene、retained full-editor root、strict coverage recovery、
RawInput pointer resolution、status-bar の review follow-up である。

legacy opaque host-token decode の挙動は維持する。明示的な command-family identity
は additive versioned envelope だけで運び、未知 version と重複する明示 family は
fail closed とする。

公開は release commit の merge 後に GitHub Actions から行う。ローカルの
`cargo login` / `cargo publish` はリリース経路に含めない。
公開確認後は `release-local-cleanup` でdefault branchへ戻してmerged release branchを
安全に削除し、branch-hygieneの実査で統合済みstashだけをdropする。Actions上のcleanupを
ローカルworktree/stash整理の代用にはしない。

## 完了台帳

- [x] 1. ローカル差分、既存 branch、対象 issue、依存更新を `release/v0.3.2` へ統合する。
- [x] 2. 通常 test、fmt、locked contract、strict lib clippy を通過する。
- [x] 3. 最新 source の strict coverage で lines/functions 100% と uncovered 0 を達成する。
- [ ] 4. commit、push、PR、CI、`master` merge を完了する。
- [ ] 5. GitHub Actions による tag、GitHub Release、crates.io 公開と不要 branch/worktree/stash 整理を完了する。

完了率は、検証証跡を満たした最上位ステップだけを数える。進行中ステップの
内部修正量や部分テスト結果は完了数へ加えない。現在は `3 / 5 = 60%` である。

## User Review Phase

- [/] 進捗率を内部作業量で水増しせず、最上位ステップの完了数だけで報告する。
- [x] strict coverage の反復経路を最終 clean gate から分離し、100% 閾値を維持したまま
      build artifact を再利用できる公式コマンド、速度比較、最終 clean gate との同値確認を追加する。
      `release-check` と通常・反復 coverage は、全6 KUC package の fmt と約2秒の AST lint を
      長時間 test/coverage より先に実行する。現行adapter unit 674件は17.15秒、
      floating/search artifact integration 4件は0.79秒、host-root 13件は0.49秒で通過した。
      4 CPU container で12 test threadsへ増やした実測は738.86秒へ悪化したため採用しない。
      長時間scenarioを除外した経路は150.88秒まで短縮した一方でlines 97.73%へ低下したため
      正式gateには採用しない。adapter-only baseline/supplementもStorybook/consumerが通る
      production経路を含まず全体gateと同値でないため破棄した。正式な補完経路は全6 packageの
      test/reportが完走し構造検査済みのprofileだけを使用し、全production source、manifest、lock、
      toolchain設定、coverage script/container wrapper/imageの署名一致を必須とする。profile-readyと
      strict-passは別状態として記録し、99%の有効profileでも公開はfail closedのまま、未達対象だけを
      補完できる。補完後はpackage-filtered `cargo llvm-cov report` で全6 packageを再集計し、strict
      JSON checkerが100%を確認した場合だけstrict-passを確定する。test/report途中で失敗した
      full run/supplementはprofile自体を無効化する。`release-check`はprofileを公開根拠にせず、常に
      全6 packageのclean full coverageを再実行する。
      12 test threadsが遅化した実測に従い、既定値は4 threadsへ固定する。
      coverage専用のproduction変形を独立レビューで検出して撤回したため、旧2106秒の100%結果は
      現在ソースの最終証跡には使わず、全6 package clean gateを再実行する。
      release PRは通常CIとrelease-preflightでstrict coverageを二重実行せず、後者の
      `release-check` だけを正とする。通常PRと`master` pushのcoverageは維持する。
      最終 `release-check` は通常Rust test 2931件/97 suites、strict coverage
      functions 12558/12558、lines 130019/130019、uncovered 0、package、publish dry-runまで通過した。
      clean全量coverageは2189秒。BuildKitのvolatileなattestation manifest IDは署名へ使わず、
      Architecture/OS/Variant/RootFS/Configからfail-closedに生成するruntime fingerprintを採用した。
      同じruntime内容の再build後も確定profileへhost-rootの実テスト1件を追加し、全6 packageを
      44秒で再集計して同じ100%を維持した。反復時間はclean全量比で約50倍短縮した。
      最新headで検出したdiagnostics focus契約4件は、全量coverageを再実行せず対象integrationを
      macOSで1.42秒、同じRust 1.97.1/Linux/Xvfb/cargo-llvm-cov環境で21.28秒
      （test本体0.04秒）で再現・修正検証した。最終公開判定は引き続きclean全量gateだけを正とする。
      最新sourceでは affected diagnostics/status integration と unit filter を23〜27秒で反復し、
      diagnostics input 176/176、status popover 122/122、progress 142/142、render 226/226を確認した。
      全adapter unitを含める診断runは333.42秒だったため反復経路には採用せず、最終判定だけを
      clean全量に固定した。残存review修正後の短期gateはcore 404件、adapter 720件/3 suites、
      AST lint、fmt、strict lib clippyを通過し、alignment moduleのfocused coverageは66/66 linesだった。
      最終公開候補sourceの `release-check` はRust test 3003件、strict coverage functions
      12610/12610、lines 130634/130634、uncovered 0、未達ファイル0、package、publish dry-runまで通過した。
      clean coverageは、Cargoが列挙した全127 test executableを最大2本、各2 test threadsでfail closedに
      実行し、Xvfb起動からstrict-pass確定まで1366.422秒だった。従来の1904.129秒から28.2%、
      初期baselineの2189秒から37.6%短縮し、100%閾値と全6 packageの対象範囲は維持した。
