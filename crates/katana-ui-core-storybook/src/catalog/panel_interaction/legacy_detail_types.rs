#[derive(Debug, Clone, PartialEq, Eq)]
pub struct StoryDetailContent {
    pub page: String,
    pub settings: String,
    pub state: String,
    pub event: String,
    pub action: String,
    pub preset: String,
    pub quality: String,
}
