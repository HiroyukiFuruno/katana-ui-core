## 0. 設計基準と互換性

- [x] 0.1 KLE v0.1.0 full-editor ownership を再監査し、generic text surface/gutter/annotation/IME/accessibility renderer は KUC、editor-domain mapping は KLE、Markdown/file/host lifecycle は KatanA と固定する
- [x] 0.2 existing `TextArea` public DTO/action/event の exhaustive consumer compatibility を確認し、new additive TextSurface wrapper を採用する
- [x] 0.3 `platform-text-raster-runtime` と `kuc-command-chrome-runtime` の dependency / shared adapter crate ownership を固定する

## 1. Renderer-Neutral TextSurface Contract

- [x] 1.1 `TextSurfaceProps` / state / action / event / frame-record と generic clipboard/history request を追加し、TextArea/text selection/text span/raster layout を compose する
- [x] 1.2 platform text-raster layout の grapheme conversion、visible span、selection/caret/preedit/hit-test を同一 surface result から構成し、Japanese/`⭐️`/ZWJ/combining mark test を追加する
- [x] 1.3 generic annotation model と deterministic overlap ordering を追加し、syntax/search/diagnostic/active-line semantics を KUC enum に入れないことを contract test で検証する
- [x] 1.4 generic line gutter rows/markers/hit events を追加し、text line box と gutter geometry が同一 frame record であることを数値 test で検証する。caller-defined visual role、injected semantic paint、host-injected accessibility description を actual egui draw と AccessKit node まで検証済み
- [x] 1.5 copy/cut/paste/undo/redo request と read-only/selection rule を追加し、KUC が clipboard/history side effect を持たないことを unit test で検証する
- [x] 1.6 TextSurface accessibility tree を追加し、focus/read-only/selection/gutter/context target/disabled reason を renderer-neutral contract test で検証する
- [x] 1.7 existing TextArea API/serde/exhaustive consumer fixture が source-compatible であることを compile/regression test で検証する
- [x] 1.8 existing `TextArea` enum を変更せず、TextSurface wrapper に layout-backed pointer/drag selection、scroll、context target、IME cancel action/event と grapheme-to-byte query を追加し、adapter が manual measurement / offset reconstruction を行わない contract test を追加する

## 2. Shared KUC Egui Adapter

- [x] 2.1 `katana-ui-core-egui-adapter` workspace crate を追加し、core/host dependency boundary と feature wiring を compile test で固定する
- [x] 2.2 adapter-owned rasterizer/texture/focus/drag/IME/accessibility state を実装し、TextSurfaceFrameRecord から RGBA texture/draw/hit target を生成する
- [x] 2.3 actual egui pointer/drag/keyboard/IME/scroll/context-menu/clipboard-history shortcut を TextSurface typed event へ変換し、real interaction test を追加する。scroll 済みの可視行 pointer hit-test から `⭐️` の IME preedit/commit までを同じ actual-egui surface で検証する
- [x] 2.4 actual AccessKit output を renderer-neutral accessibility tree と照合し、focus/selection/read-only/gutter/context-menu の query test を追加する。root/input/gutter/context、surface bounds、host-injected gutter description と scroll 済み可視 TextRun bounds を実証する
- [x] 2.6 allocated `surface_bounds`、clipped `viewport_bounds`、text `content_bounds` を同一 frame record に追加し、background/pointer/AccessKit/artifact が `surface_bounds` を共有する actual-egui test を追加する。adapter-owned immutable PaintPlan と artifact frame が同じ bounds を保持し、Storybook PNG/GIF encoder まで接続済み
- [x] 2.7 one TextSurface scroll state を raster layout/gutter/annotation/selection/caret/hit-test/IME/AccessKit に反映し、actual wheel input の frame-to-frame pixel/bounds contract を追加する。actual wheel、viewport stability、content shift、上端/下端 clamp、scroll 済み pointer hit-test/IME/AccessKit と gutter/annotation/selection/caret の同一座標系、および same-surface Storybook artifact の pixel contract を実証済み
- [x] 2.7a `TextSurfaceViewportSizing` を導入し、既定 `Fixed` と adapter 測定 `AdapterMeasured` を構築時 builder で切り替えられるようにする。fixed は `min(available, configured width)` / `configured height` の既存挙動を維持し、adapter 測定のみ `synchronize_measured_viewport_size` が呼ばれるようにする。
- [x] 2.7a-1 `Fixed` は呼び出し側指定 `viewport` を preserve し、`AdapterMeasured` は adapter-owned
  `surface_extent_for_ui` で `ui.available_width/height` を測定して `TextSurface` の measured viewport を更新する。
- [x] 2.7a-2 RawInput evidence:
  - `actual_egui_viewport_measurement_uses_available_region_and_preserves_interaction_state`
  - `actual_egui_viewport_measurement_with_pointer_focus_preserves_scroll_ime_accesskit`
  - `actual_egui_scrolled_pointer_hit_test_and_ime_commit_share_one_surface_layout`
- [/] 2.8 caller-defined gutter visual role と injected semantic paint を additive に追加した。active/hovered/marker/diagnostic 相当を visual role だけで表したが、active/hovered の resolved frame fact、controlled hover input、same-state paint/AccessKit contract は未完了であり 3.1g-11 で完了する
- [x] 2.5 `egui::TextEdit`、font registration/measurement、OS font lookup、manual glyph/gutter renderer、Unicode/emoji fallback を adapter/KLE text-surface path から禁止する AST/dependency test を追加する

## 3. Storybook and Consumer Migration

- [x] 3.1 KUC Storybook TextArea を shared adapter の live TextSurface へ置換し、scripted event sequence/frame-record/numeric jitter test を追加する
  - [x] 3.1a Storybook 専用の `eframe` TextSurface runtime を設計どおり追加し、`--open-window text-area` が existing Canvas preview ではなく actual `EguiTextSurfaceAdapter::show` path を開くことを compile/runtime contract で固定する
  - [x] 3.1b adapter-owned `TextSurfacePaintPlan` / `TextSurfaceArtifactFrame` を追加し、egui painter と deterministic PNG/GIF/manifest encoder が同一 immutable plan を消費することを test で固定する。Storybook-owned layout/glyph/scroll/caret renderer を追加しない
  - [x] 3.1c Japanese/`⭐️`/gutter/annotation fixture と focus/wheel/pointer/IME/copy/history/context script を actual egui event として実行し、frame record、typed event、AccessKit summary を step ごとに保存する
  - [x] 3.1d deterministic GIF/manifest/PNG と numeric jitter/pixel/record-hash gate を追加し、same input の再実行一致、scroll bounds 一貫性、`⭐️` variation selector/color output、fallback source 不在を検証する
  - [x] 3.1e KUC guardrail/AST lint を更新し、TextSurface Storybook の Canvas fallback、manual glyph/geometry、core action direct script、shape-count acceptance を拒否する
- [x] 3.1f KUC CommandChrome Storybook を actual `EguiCommandChromeAdapter::{show_toolbar,show_floating_toolbar,show_search_strip}` path に接続し、Katana parity fixture の inline/heading/list/code-dropdown/image-like/floating/search-replace controls を generic injected command data で表示する。KUC core/adapter に Markdown/KatanA/KLE/KDV semantics を追加しない
  - [x] 3.1f-1 toolbar / floating toolbar / search strip の final record、platform raster/SVG texture、panel/tooltip/dropdown と search input/control を adapter-owned immutable `CommandChromePaintPlan` / artifact frame に追加し、actual egui painter と同じ plan を消費する contract test を追加した。search strip は shared TextSurface paint plan を compose し、直接 painter を持たない。PNG/GIF/manifest encoder は 3.1f-3 で実装する。TextSurface plan は compose だけにし、Storybook-owned layout/glyph/geometry renderer を追加しない
  - [x] 3.1f-2 generic consumer-provided floating visibility と `SearchControlStrip` stable state id の additive contract を先に追加し、その state を注入する live eframe window と headless actual-egui `RawInput` script を追加する。hover/disabled/activation/split dropdown pointer+keyboard+dismiss/floating tooltip+focus-return/search-replace capability/IME Japanese+`⭐️` を typed event と AccessKit node まで検証する。Storybook script は core `apply_action` 直呼びで state を作らず、open gesture は KLE/KatanA selection integration で検証する。独立 fixture run の record/plan/RGBA hash は一致しなければならない
  - [x] 3.1f-3 numbered PNG、deterministic GIF、manifest、record/plan/RGBA pixel hash、bounds/focus/dropdown/accessibility summary を出力し、re-run一致、disabled no-event、close reason、variation selector/color texture を numeric gate で検証する
  - [x] 3.1f-4 AST/KUC guard を更新し、CommandChrome Storybook の Canvas fallback、manual glyph/icon/geometry、native text painter/font lookup、emoji replacement/fallback、direct core action script、KUC core への Markdown/host dependency を拒否する
- [x] 3.1g KLE/KDV のような controlled consumer が generic UI state/geometry を再実装せずに KUC components を利用できる additive synchronization contract を追加する。KUC core/adapter に Markdown/KatanA/KLE/KDV semantics を追加せず、synchronization は interaction event を合成しない。
  - [x] 3.1g-1 `TextSurface` controlled presentation synchronization を追加する。external source value、UTF-8 byte selection/caret、input/read-only policy、span、annotation、gutter/accessibility presentation を同期しても KUC-owned focus/drag/scroll/IME/preedit/layout/texture state を保持する。public API は source-compatible additive とし、consumer が private action/core `apply_action` で状態を偽装する必要をなくす。
  - [x] 3.1g-2 automatic numbered-gutter mode を追加する。KUC text layout から logical row/display label/row bounds/scroll-aligned raster/pointer/AccessKit target を生成し、consumer には sparse opaque row marker/visual-role/accessibility override だけを許可する。consumer row enumeration、line-number paint、row coordinate は guard で拒否する。
  - [x] 3.1g-3 `CommandChromeToolbar` / `FloatingCommandToolbar` / `CommandChromeSearchStrip` controlled presentation synchronization を追加する。opaque action/group/item data、localized presentation、enabled/selected/visible state、query/replace/options/result/capability を event なしで同期し、KUC-owned focus/dropdown/tooltip/input identity を保持する。
  - [x] 3.1g-4 floating toolbar の actual panel self-measurement and placement を adapter に固定する。consumer は current `TextSurfaceFrameRecord` 由来の anchor/viewport facts と visibility policy だけを渡し、KUC adapter が panel dimensions、clamp、outside/Escape、focus-return を所有する。consumer popup-coordinate/panel-size algorithm を guard で拒否する。
- [x] 3.1g-5 actual `RawInput` / AccessKit / frame-record / pixel / artifact hash tests を追加する。external update during IME、Japanese/`⭐️`/ZWJ、automatic gutter multiline edit+scroll、controlled search/replace focus preservation、floating resize/scroll/outside/Escape/focus return、disabled no-event、deterministic rerun を検証する。core `apply_action` direct script、manual geometry、fallback pixel を acceptance evidence にしない。
- [x] 3.1g-6 automatic numbered gutter に range-anchored marker override を追加する。consumer は UTF-8 byte range、start-anchor policy、opaque marker id、priority、semantic visual/accessibility data、optional host `UiIconProps` だけを渡す。KUC は current layout から start row を再解決し、priority/input order で一意に選ぶ。existing KUC-issued row-id override は維持する。invalid/non-boundary range は panic せず無視する。
- [x] 3.1g-7 range-anchored marker を KUC SVG raster runtime だけで paint/cache し、同一 frame record の distinct marker bounds/hit target/AccessKit/artifact に投影する。Japanese/`⭐️` VS16/multiline/source-update/priority/RawInput pointer/color texture と deterministic artifact hash を actual adapter test で検証する。legacy non-icon marker は source compatibility のため whole-row marker activation と bounds を維持する。
- [x] 3.1g-8 controlled `TextSurfaceScrollRequest` を追加する。opaque request token、logical row / UTF-8 byte offset / byte range / relative pixel target、nearest/start/center/end alignment を consumer input とし、KUC layout/adapter が range-to-rect、scroll mutation/clamp、same-token no-op、new-token replacement、typed acknowledgement/rejection、frame/artifact record を所有する。actual RawInput で Japanese/`⭐️` multiline、wheel後の保持、fresh token、AccessKit/record/artifact hash を検証した。bounds 未確定 frame は token を消費せず、同一 token が bounds 同期後に解決されること、および無効 UTF-8 target が interaction/raster state を変更せず typed rejection を返すことも core と actual adapter で検証した。
- [x] 3.1g-9 controlled `TextSurfaceFocusRequest` を追加する。opaque request token と requested focused state を consumer input とし、KUC adapter が actual allocated response に対する focus/surrender issue、same-token no-op、new-token replacement、typed acknowledgement、frame/artifact event を所有する。request issued と next-frame actual focus synchronization を区別し、Japanese/`⭐️`/IME/preedit/selection/scroll/raster state を保持する actual RawInput/AccessKit/artifact test を追加する。consumer は egui focus memory や direct core action を呼ばない。
- [x] 3.1g-10 `RelativePixels` の f32 transport value と KUC-owned scale/rounding/clamp、frame-derived `visible_logical_rows` を追加した。consumer は i32 丸め、residual、newline enumeration、pixel-to-line conversion を持たない。non-finite transport は normalized IEEE-754 bit-pattern JSON wire form で round-trip し、typed rejection となる。empty/partial/multiline Japanese/`⭐️`/scroll/automatic gutter の core/actual RawInput/AccessKit/frame/artifact test、same-token no-op、fresh-token single acknowledgement applied offset、state/raster 不変、AST guard を検証した。
- [x] 3.1g-11 automatic numbered gutter の active/hover state を KUC frame contract として完了する。KUC は current `TextArea` caret と current layout から active row を導出し、consumer は labels/bounds/geometry を持たない deduplicated logical-row hover input だけを渡す。`TextSurfaceGutterFrame`、AccessKit target、adapter paint plan、artifact record が同じ active/hover/marker/bounds を返し、invalid row は KUC が layout resolution 時に無視する。multiline Japanese/`⭐️` VS16、caret move、controlled hover update、range marker pointer hit、scroll、source replacement の core/actual `RawInput`/AccessKit/pixel/hash/AST tests を追加し、KLE/KDV/KatanA 固有 enum/colour/diagnostic semantics、consumer line enumeration、manual geometry を拒否する。
- [x] 3.1g-12 `katana-ui-core-egui-adapter` に public `artifact_compositor` を追加し、actual root `ArtifactCanvasBounds` と ordered `TextSurfacePaintPlan` / `CommandChromePaintPlan` refs だけから deterministic RGBA/pixel hash/plan hash/non-transparent count を返す。KUC が clipping、RGBA length validation、nearest sampling、source-over blend、canvas-relative indexing、overflow を所有し、invalid texture/zero canvas/overflow は typed error にする。KUC Storybook の private text/command-chrome blend/composite source をこの public API に atomically 移行して削除し、KLE/KDV/Markdown/KatanA semantics、layout/glyph/geometry union、PNG/GIF I/O を adapter API に入れない。pure clipping/alpha/error/re-run tests と Japanese/`⭐️` VS16 TextSurface + toolbar + floating + search strip の actual-egui RawInput/AccessKit/record/colored-texture/RGBA hash contract、AST guard を追加する。
- [x] 3.1g-13 existing generic `ContextMenu` core model を shared actual-egui adapter へ接続する。controlled opaque item-tree presentation、TextSurface が actual secondary-click 又は keyboard/AccessKit selection/caret から出す immutable context-target anchor、retained focus/type-ahead/submenu、self-measure/clamp/outside/Escape/focus-return、AccessKit、`ContextMenuPaintPlan`/artifact を KUC に追加する。consumer pixel DTO、egui menu/Area、Markdown/KLE/KDV/KatanA/document/clipboard semantics を禁止し、KUC artifact compositor の additive ContextMenu plan-ref で同一 frame RGBA を作る。right-click/keyboard/AccessKit、Japanese/`⭐️` VS16 label、nested code kind、disabled no-event、placement/focus close、actual record/plan/colored texture/RGBA repeat/AST guard を検証する。
- [x] 3.1g-14 generic retained `EguiTextCommandSurfaceAdapter` を KUC adapter に追加する。actual root `egui::Ui` のみから controlled TextSurface / floating toolbar / search-strip を一つの stable surface として組み、KUC が search/tool slots の予約、TextSurface viewport allocation、same-frame child event/focus/scroll/artifact output と paint order を所有する。consumer root/child rect、height subtraction、manual child `Ui`/overlay、alternate search widget、KLE/KDV/KatanA/Markdown/document/search-provider semantics を禁止する。Japanese/`⭐️` VS16/IME/edit/selection/floating/search-replace focus/outside-Escape/focus-return の actual RawInput/AccessKit/reachable-bounds/repeatable artifact test と consumer layout bypass AST guard を追加する。3.1g-15 の consumer-safe retained model と deferred bootstrap を含めて主担当再検証済み。
- [x] 3.1g-15 3.1g-14 を consumer-safe retained composition へ補正する。public root path は TextSurface/optional root toolbar/optional floating toolbar/optional search strip を KUC 内で一括保持する単一 `EguiTextCommandSurface`（又は同等の既存KUC命名）だけを可変 input とし、separate mutable `CommandChromeToolbar` と `FloatingCommandToolbar` の同時受領を禁止する。consumer は child model borrow なしに KUC-owned controlled toolbar/floating/search presentation を同期できる。floating の初回 anchor/viewport/panel/focus-return は same-frame actual TextSurface fact から KUC が deferred に初期化し、以後の selection/caret change も同じ retained floating state を KUC が same-frame fact で再配置する。consumer の `Rect`/viewport/`UiNodeId`/previous frame/placeholder を禁止する。optional child は未表示時に root slot を占有しない。foreign-consumer compile/actual RawInput/AccessKit/reachable bounds/artifact hash/AST tests で初回および移動 selection toolbar、Japanese/`⭐️` VS16/IME/search-replace/outside-Escape/focus-return を実証し、KLE/KDV/KatanA/Markdown/document/search semantics と consumer layout bootstrap を拒否する。
- [x] 3.1g-16 generic floating dropdown の visible hit region と outside-dismiss precedence を補正する。KUC adapter は current floating panel、dropdown trigger/menu、enabled item bounds を一つの retained generic interaction region として持ち、root 内の最終 visible item を含む actual pointer press/release を outside close より先に typed dropdown activation へ解決する。activation は1回、disabled item は inert、真のoutside はclose、Escapeはmenuから先にcloseする。consumer coordinate/hit test/event synthesis、KLE/KDV/KatanA/Markdown/document semantics を禁止し、17 code-kind item、Japanese/`⭐️`、AccessKit/record/artifact hash、separate press/release RawInput、AST guardを追加する。
- [x] 3.1g-17 retained generic text-command-context root を追加する。public root は retained TextSurface/optional root toolbar/optional floating toolbar/optional search strip/optional ContextMenu を一つの KUC model に保持し、one `show` call だけで same-frame TextSurface context-target から optional context menu を開く。KUC が root allocation、context overlay paint/compositor order、right-click/keyboard/AccessKit request、submenu/type-ahead、self-measure/clamp、outside/Escape/focus-return、record/artifactを所有する。consumer は opaque controlled item tree/visibilityだけを同期し、anchor/Rect/Ui/focus ID/height reservation/menu geometry/hit test/overlay order/compositor planを持たない。KLE/KDV/KatanA/Markdown/document/path/clipboard semanticsを禁止し、Japanese/`⭐️` VS16、nested/disabled、optional absence、secondary-click/keyboard/AccessKit、outside/Escape/focus-return、same-root AccessKit/record/artifact/RGBA repeat と consumer bypass AST guard を実証する。
- [x] 3.1g-18 retained text-command-context root の immutable artifact-layer order を public output に追加する。KUC root が Text/toolbar/search/floating と optional ContextMenu artifact を一つの owned generic sequence にして、ContextMenu artifact が存在する時は必ず最終 layer にする。KUC のみが layer から paint-plan ref を解決し、consumer は sequence を clone/forward するだけで insert/delete/sort/reverse/infer/merge できない。KLE/KDV/Storybook の manual context order/compositor を AST で拒否し、actual RawInput right-click/keyboard/AccessKit と optional absence、Japanese/`⭐️` VS16、RGBA repeat hash で public sequence と compositor output が一致することを検証する。
- [x] 3.1g-19 generic `ContextMenu` overflow contract を実装する。KUC が retained vertical scroll、wheel input、viewport clamp、visible-row clipping、focus/AccessKit/artifact/compositor を一体で所有し、`EguiContextMenuFrameRecord::items` には current `record.bounds` 内で physical hit-test 可能な行だけを返す。consumer の offset/geometry/hover/hit-test/action fallback を禁止する。secondary-click / Shift+F10 / AccessKit の各 route で、long direct submenu と nested 17-item submenu の最終 enabled leaf を actual scroll、fresh public record bounds、primary press/release で一回だけ selection すること、disabled leaf の inertness、outside/Escape/focus-return、Japanese/`⭐️` VS16/ZWJ、AccessKit、clipped paint/artifact hash の不変性を actual egui test で検証する。
- [x] 3.1g-19a overflow submenu transition の回帰を追加する。KUC は `smooth_scroll_delta` を次 frame または別 submenu path に再適用せず、actual `RawInput::events` の current-frame `MouseWheel` だけを retained offset に反映する。Save/Format/Edit/Ingest の四根で Edit が index 2 の実ツリー、Edit の 13 direct と code submenu、code の 17 generic kinds を secondary-click / Shift+F10 / AccessKit で開き、Edit を scroll 後に refreshed public record の code submenu を physical click、次の public record の `code.text` を primary press/release して `ContextMenuEvent::ItemSelected` を一回だけ受ける。KLE 固有 id/label/geometry は production code に追加しない。
- [x] 3.1g-20 first staged closed root contract を `EguiTextCommandSurfaceRoot` として追加する。KUC 内部の TextSurface、gutter/diagnostic presentation、command chrome、search、ContextMenu、artifact compositor を一つの retained root で構成し、consumer へは root identity/state revision/dimensions/RGBA・plan・record hash/AccessKit reference を持つ closed frame と、hash/cardinality だけを持つ closed event batch を返す。child artifact、artifact order、paint plan、texture/egui id、child geometry、KatanA/KLE/Markdown/search semantics は公開しない。同一 `RawInput` trace の root hash/event 再現性と public surface 閉鎖契約を自動検証する。emoji/IME provisioning、Storybook full-root、KLE binding は後続 task の対象とする。
- [ ] 3.2 KLE `PlatformTextSurface` / `LineGutterModel` / generic egui overlay を KUC adapter binding に置換し、暫定 local modules を削除する
- [ ] 3.3 KLE の syntax/search/diagnostic/context/authoring binding を KUC TextSurface annotations/gutter/ContextMenu/DiagnosticsList/CommandChrome へ map し、KLEに generic renderer/state machine が残らないことを検証する
- [ ] 3.4 KLE Storybook interactive window、GIF/manifest、acceptance artifact を shared TextSurfaceFrameRecord へ置換し、fallback renderer を削除する
- [ ] 3.5 KatanA actual UI tests で Japanese/`⭐️` IME、selection/authoring cursor restore、copy/cut/paste、undo/redo、shortcut、accessibility、line gutter、search/replace を KLE/KUC integration として実行する

## 4. Guardrails and Release Evidence

- [x] 4.1 KUC `kuc_guardrails` / AST lint に shared text-surface adapter ownership、no core egui dependency、no private renderer を追加する
- [ ] 4.2 KLE/KDV guard/release gate に duplicate text surface/gutter/font resolver/raster cache/fallback artifact を追加し、existing `platform-text-raster-runtime` migration task 5.1 と統合する
- [ ] 4.3 formatter、clippy、workspace tests、KUC guardrails、AST lint、OpenSpec strict validate、KLE/KDV/KatanA targeted tests を requirement-to-evidence matrix へ記録する
- [ ] 4.4 KUC/KLE/KDV/KatanA の責務境界を自己レビューし、未対応 feature を `Unsupported` や screenshot-only evidence で隠していないことを確認する
