# Tasks

- [x] 1. ColorPicker の未完了フィードバックを OpenSpec に明文化する
- [x] 2. インライン展開をやめ、前面表示（overlay）で開くパネルに変更する
- [x] 3. 外側クリック、Esc、閉じるボタンで確実に閉じるようにする
- [x] 4. トリガーは数値なしの色ボタンをデフォルトにし、RGBA 時は左に透過プレビュー、右に不透明 RGB を表示する
- [x] 5. RGB / RGBA の各値を利用者が操作できる UI にする
- [x] 6. 色面、色相、透明度の見た目を改善し、格子が目立つ表示を避ける
- [x] 7. Storybook で開閉、値変更、RGB 専用、RGBA、readonly、disabled を確認できるようにする
- [x] 8. `just storybook-check` を warning-as-error で通す
- [x] 9. 未実装のスポイト操作をデフォルト表示から除去する
- [x] 10. 透明度スライダーをドラッグ中も連続更新できるようにする
- [x] 11. 初回表示時の色面に網線が出ないよう、色面描画から透明度を切り離す
- [x] 12. デフォルト表示サイズを 25% 縮小し、呼び出し側からサイズ指定可能にする
- [x] 13. 閉じるボタンの `×` を上下左右中央に揃える
- [x] 14. 色相バーの縦線が見えないよう、グラデーションをシームレスに描画する
- [x] 15. 色ボタントリガーに xs / sm / mid / large / xlarge のサイズ指定を追加する
- [x] 16. 色ボタントリガーの外枠ありなしを指定できるようにする
- [x] 17. 色ボタントリガーの二重枠を廃止し、枠は親ボタンだけに持たせる
- [x] 18. 色ボタントリガーのカスタム描画ビューを廃止し、Adapter の通常Node構成でクラッシュを回避する
- [x] 19. パネルの `title` は固定値を廃止し、利用側指定（未指定可）で表示できるようにする
- [x] 20. Storybook の RGBA サンプルで `title` を利用側から渡す例と、値の別ラベル表示を追加する
- [x] 21. RGBA トリガーの左透過プレビュー / 右不透明 RGB プレビューを維持しつつ、右端はみ出しを防ぐ
- [x] 22. 色相バーをよりシームレスなグラデーション描画に調整し、初回表示時の格子の主張を抑える
- [x] 23. `just storybook-check` / `just ast-lint` / `STORYBOOK_SMOKE_PAGES=\"color-picker-rgba\" just storybook-smoke` を実行して通過を確認する
